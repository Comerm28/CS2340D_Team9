<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SprintProject"
        tools:targetApi="31">

        <!-- Start screen -->
        <activity
            android:name="com.view.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:exported="true">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Start screen -->
        <activity
            android:name="com.view.StartScreenActivity"
            android:exported="true" />

        <!-- Login screen -->
        <activity
            android:name="com.view.LoginActivity"
            android:exported="true" />

        <!-- Account creation screen -->
        <activity
            android:name="com.view.CreateAccountActivity"
            android:exported="true" />

        <!-- Main app screen -->
        <activity
            android:name="com.view.MainAppScreensActivity"
            android:exported="true" />


    </application>
</manifest>
